/* write a program which reads in (using the keyboard) 
 seven real values to an array named valueMe, computes the
  sum and average then display them on the screen*/ 
#include <stdio.h>
int main()
{
  int a;

 
  for(a=11;a<=20;++a)
  {

    
    if (a==12)
    { 
        continue;
    }
        printf("a=%d",a);
  }
}

